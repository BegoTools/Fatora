# Feature Expansion — Customer Statements, Reports, Invoice Printing, Item Images, Branding & Theming

## السياق (Context)

النظام الحالي (نظام إدارة صيدلية/متجر) محتاج إضافة مجموعة ميزات جديدة مترابطة ببعض. الهدف من هذا الـ prompt إنه يكون تنفيذي وواضح لأي AI coding agent (Claude Code / Cursor / إلخ) يشتغل عليه بدون لبس.

---

## Phase 0 — أسئلة التوضيح الإلزامية (Clarifying Questions Gate)

قبل كتابة أي كود، لازم الـ agent يسأل الأسئلة دي ويستنى الإجابة:

### 1. كشف حساب العميل (Customer Statement)
- هل كل عميل عنده رصيد واحد إجمالي (مديونية/دائنية)، ولا لازم نفصّل كل حركة برصيدها بعد الحركة (running balance)؟
- الحركات المفروض تظهر في الكشف: فواتير، مدفوعات، تعديل يدوي (manual adjustment)، أي حاجة تانية؟ لكل نوع حركة هل محتاج حقل "سبب" نصي حر ولا قائمة أسباب محددة مسبقًا (dropdown)؟
- هل التعديل اليدوي على الرصيد (زيادة/نقصان) يحتاج صلاحية خاصة (مثلاً admin only)؟
- هل نحتاج فلترة الكشف بفترة زمنية (من - إلى)؟

### 2. التقارير (Reports)
- إيه أنواع التقارير المطلوبة تحديدًا؟ (مبيعات يومية/شهرية، مخزون، ديون عملاء، أرباح... إلخ) — محتاجين قائمة واضحة.
- صيغة الملف المطلوبة: PDF ولا Excel ولا الاتنين؟
- هل التقرير بيتحفظ محليًا فقط ولا فيه إمكانية مشاركة (واتساب/إيميل)؟

### 3. طباعة آخر فاتورة (Reprint Last Invoice)
- "آخر فاتورة" يعني آخر فاتورة تم إنشاؤها في النظام كله، ولا آخر فاتورة لكل عميل على حدة؟
- الطباعة بتتم على طابعة عادية (A4) ولا طابعة فواتير حرارية (thermal/receipt printer)؟

### 4. صورة الصنف (Item Image)
- "تتحفظ local على جهاز المستخدم" — يعني الصورة بتتخزن كـ file path على جهاز الأدمن اللي بيدخلها بس (وبالتالي مش هتظهر لباقي الأجهزة/المتصفحات)، ولا المقصود تخزين كـ base64/blob جوه الـ local database (SQLite/IndexedDB) بحيث تفضل موجودة حتى لو الجهاز اتغير؟ ده مهم جدًا يتحدد لأنه بيأثر على المعمارية بالكامل.
- في حالة مفيش صورة، إيه الـ placeholder المطلوب؟

### 5. الرصيد السابق على الفاتورة الجديدة (Previous Balance on New Invoice)
- المطلوب إن الفاتورة الجديدة تعرض: "حساب سابق: [المبلغ] — فاتورة رقم [X]" — هل المقصود بـ "فاتورة رقم X" آخر فاتورة كان عليها رصيد، ولا كل الفواتير الغير مسددة (لو أكتر من فاتورة)؟
- الرصيد السابق ده بيتجمع مع قيمة الفاتورة الحالية في الإجمالي النهائي المطبوع، صح؟

### 6. الشعار (Logo) وتخصيص الألوان (Theme)
- الشعار: صورة واحدة تتحفظ في إعدادات النظام وتتسحب في الـ header بتاع التطبيق + في الفاتورة المطبوعة؟
- تخصيص الألوان: المطلوب اختيار لون أساسي واحد (primary color) يتطبق على النظام كله، ولا theme presets جاهزة (فاتح/غامق + عدة ألوان)، ولا color picker حر بالكامل؟

### 7. تصدير الأصناف / الجرد (Inventory Export)
- المطلوب تصدير كل الأصناف (الجرد الكامل) لملف Excel، ولا فيه إمكانية اختيار أصناف معينة/فلترة (بالفئة، بالمخزون المنخفض، إلخ) قبل التصدير؟
- الأعمدة المطلوبة في ملف الجرد: اسم الصنف، الكمية، سعر الشراء، سعر البيع، الفئة، أي حاجة تانية؟
- المطلوب Excel محلي (.xlsx يتحفظ على الجهاز) بس، ولا كمان رفع مباشر لـ Google Sheets (يعني ربط عن طريق Google Sheets API/OAuth)؟ ده مهم لأنه بيفرق كتير في التنفيذ (ملف محلي بسيط vs. تكامل مع Google API واصلاحيات).
- هل التصدير ده حاجة تتعمل يدوي بالطلب بس، ولا محتاجين جدولة تلقائية (مثلاً كل يوم/أسبوع)؟

**Agent لازم يستنى إجابات المستخدم على الأسئلة دي قبل ما يبدأ أي تنفيذ.**

---

## Phase 1 — Database & Data Model

- إضافة جدول `customer_transactions` (أو تعديل الموجود) بحقول: `customer_id`, `type` (invoice/payment/adjustment/other), `amount`, `direction` (debit/credit), `reason`, `related_invoice_id` (nullable), `created_at`.
- إضافة حقل `image_url` أو `image_blob` لجدول `items` حسب قرار Phase 0.
- إضافة حقل/جدول `app_settings` لتخزين: `logo_url`, `primary_color` (وأي theme tokens تانية).
- إضافة حقل `previous_balance_invoice_ref` على جدول `invoices` لربط الفاتورة الحالية بالفاتورة السابقة صاحبة الرصيد.

## Phase 2 — كشف حساب العميل (Customer Statement)

- شاشة عرض كشف حساب لكل عميل: قائمة حركات بالترتيب الزمني، عمود مدين، عمود دائن، عمود رصيد متراكم (running balance)، وعمود السبب.
- فلترة بالفترة الزمنية.
- زرار تصدير الكشف (PDF/Excel حسب قرار Phase 0).
- شاشة/مودال لإضافة حركة يدوية (تعديل رصيد) بسبب نصي إجباري.

## Phase 3 — الفاتورة والرصيد السابق

- عند إنشاء فاتورة لعميل عليه رصيد سابق (مديونية):
  - عرض سطر "حساب سابق" في الفاتورة (شاشة + طباعة) بالمبلغ ورقم آخر فاتورة مرتبطة.
  - إضافة المبلغ ده للإجمالي النهائي المطبوع.
- عند حفظ الفاتورة: تسجيل حركة جديدة تلقائيًا في `customer_transactions` بنوع `invoice` وربطها بـ `related_invoice_id`.

## Phase 4 — طباعة آخر فاتورة

- زرار "طباعة آخر فاتورة" في الشاشة الرئيسية أو شاشة العميل حسب قرار Phase 0.
- استدعاء نفس مكوّن/template الطباعة المستخدم للفواتير العادية (عدم تكرار الكود — إعادة استخدام).

## Phase 5 — التقارير

- شاشة "التقارير" فيها قائمة بأنواع التقارير المتفق عليها في Phase 0.
- كل تقرير: اختيار فترة (لو منطبق) → توليد → تصدير ملف → حفظه محليًا في مسار واضح للمستخدم.

## Phase 6 — صورة الصنف

- إضافة حقل رفع صورة اختياري (غير إجباري) في شاشة إضافة/تعديل الصنف.
- عرض الصورة (أو placeholder) في قائمة الأصناف وفي الفاتورة إن أمكن.
- التخزين حسب القرار في Phase 0 (path محلي أو blob في local DB).

## Phase 7 — الشعار وتخصيص الألوان

- شاشة إعدادات عامة (Settings) فيها:
  - رفع/تغيير شعار الشركة → يظهر في الـ header وفي رأس الفاتورة المطبوعة.
  - اختيار اللون الأساسي للتطبيق (حسب آلية التخصيص المتفق عليها في Phase 0) → يتطبق فورًا على الـ UI (CSS variables / theme tokens، مش hardcoded colors).

---

## Phase 8 — تصدير الأصناف / الجرد (Inventory Export)

- زرار "تصدير الجرد" في شاشة الأصناف (أو ضمن شاشة التقارير — حسب ما يتحدد).
- توليد ملف Excel (.xlsx) بالأعمدة المتفق عليها في Phase 0 باستخدام مكتبة مناسبة (مثل `xlsx` / `exceljs`).
- تطبيق نفس فلاتر شاشة الأصناف الحالية (لو موجودة) على التصدير، بحيث لو المستخدم فلتر بفئة معينة يقدر يصدّر النتيجة دي بس.
- لو اتقرر ربط Google Sheets:
  - استخدام Google Sheets API مع OAuth الخاص بحساب المستخدم (مش خدمة عامة مشتركة).
  - إنشاء sheet جديد أو تحديث sheet موجود (يتحدد حسب اختيار المستخدم) بنفس بيانات الجرد.
  - التعامل مع الحالة إن التوكن انتهت صلاحيته (refresh token flow) من غير ما يفشل التصدير بصمت.
- في كل الحالات: التصدير عملية read-only على قاعدة البيانات — ممنوع أي تعديل على بيانات الأصناف أثناء التصدير.

---

## Phase 9 — التصميم الرسمي لفاتورة المبيعات (Official Invoice Layout)

مرفق ملف **`شكل_الفواتير.xlsx`** وهو التصميم الرسمي المعتمد لأي فاتورة مبيعات تطلع من النظام (طباعة أو PDF). هذا التصميم هو الأساس الافتراضي (default template)، والعميل بعد كده يقدر يعدّل الألوان والتنسيق من شاشة الإعدادات (المذكورة في Phase 7)، لكن **البنية والحقول الأساسية** المذكورة في الملف لازم تفضل ثابتة كنقطة بداية.

### محتوى القالب (حسب الملف المرفق)
القالب عبارة عن ورقة Excel فيها placeholders بصيغة `{{Variable_Name}}` لازم الـ agent يربطها ببيانات النظام الفعلية:

**رأس الفاتورة (Header):**
- اسم الشركة + مكان الشعار (`Logo`) — يتغذّى من إعدادات الشعار في Phase 7.
- `{{Company_Phone}}` — رقم هاتف الشركة.
- `{{Date_Time}}` — تاريخ ووقت الفاتورة.
- `{{Invoice_Number}}` — رقم الفاتورة (يظهر مرتين في التصميم).
- `{{Owner_Name_1}}` / `{{Owner_Name_2}}` — أسماء يحددها المالك (حقول قابلة للتخصيص من الإعدادات).
- `{{Seller_Name}}` — اسم البائع اللي عمل الفاتورة.
- `{{Admin_Name}}` — اسم الإدارة/المستخدم المسؤول.
- `{{Client_Name}}` — اسم العميل.

**جدول الأصناف (Items table):** أعمدة: كود المنتج، اسم المنتج، الكمية، السعر، الإجمالي — لكل صنف:
- `{{Item_Code}}`, `{{Item_Name}}`, `{{Item_Qty}}`, `{{Item_Price}}`, `{{Item_Total}}`
- الجدول لازم يدعم عدد أسطر متغيّر حسب عدد أصناف الفاتورة (الملف فيه صف نموذج واحد بس، لازم يتكرر ديناميكيًا).

**تذييل الفاتورة (Totals footer):**
- `{{Total_Amount}}` — الإجمالي Total.
- `{{Previous_Balance}}` — حساب سابق (هو نفسه المذكور في Phase 3 — لازم يترابط مع نفس المنطق ورقم الفاتورة السابقة).
- `{{Down_Payment}}` — تحت الحساب (المبلغ المدفوع مقدمًا في هذه الفاتورة، لو موجود).
- `{{Net_Amount}}` — الصافي Net (الإجمالي + حساب سابق - تحت الحساب).
- بيانات تواصل الشركة: إيميل، هاتف، عنوان.

### متطلبات التنفيذ
- بناء invoice template component (HTML/PDF) يطابق نفس ترتيب وبنية الحقول دي بالظبط كنقطة انطلاق، مش يبتدئ من تصميم عشوائي.
- كل placeholder في الملف لازم يتربط بمصدر بيانات حقيقي من قاعدة البيانات — لو فيه placeholder مالوش مصدر بيانات واضح، الـ agent يسأل قبل ما يكمل (نفس مبدأ Phase 0).
- التصميم ده هو الـ default عند أول تشغيل للنظام؛ أي تخصيص ألوان/خطوط من المستخدم (Phase 7) يتطبق فوق نفس البنية دي، مش بديل عنها.
- الـ layout والـ RTL alignment يفضلوا زي ما هو موضح في الملف (الاتجاه من اليمين لليسار للنصوص العربية).

---

## المبادئ غير القابلة للتفاوض (Non-Negotiable Principles)

1. **لا كود قبل الإجابة على أسئلة Phase 0.**
2. إعادة استخدام مكوّن الطباعة الموجود لأي شاشة طباعة جديدة، مش عمل نسخة تانية.
3. أي تعديل يدوي على رصيد العميل لازم يترك أثر (audit trail) بسبب واضح — ممنوع تعديل الرصيد بدون تسجيل حركة.
4. الألوان والـ branding لازم تتطبق عن طريق design tokens / CSS variables مركزية، مش قيم لون متفرقة جوه الكومبوننتس.
5. صورة الصنف اختيارية 100% — مفيش أي flow لازم يفشل أو يمنع الحفظ لو مفيش صورة.
6. أي feature جديدة تتوافق مع الـ stack الحالي (React/Next.js + Supabase أو الـ local DB المستخدم فعليًا في المشروع) من غير إدخال مكتبات تقيلة غير ضرورية.
7. ميزات التصدير (تقارير/جرد) لازم تكون read-only تمامًا على قاعدة البيانات — أي تصدير ملف ما ينفعش يعدّل أو يحذف أي بيانات فعلية.
8. تصميم فاتورة المبيعات لازم يستند لبنية ملف `شكل_الفواتير.xlsx` المرفق كنقطة انطلاق رسمية — التخصيص (ألوان/تنسيق) يُبنى فوقها ولا يُلغيها.
